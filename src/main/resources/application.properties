spring.application.name=MySpotPGManagementBackend


server.port=9000

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/myspot_project
spring.datasource.username=root
spring.datasource.password=1432
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# JWT Configuration - SECURE 512-BIT KEY
app.jwt.secret=MySpot2025SecureJWTSigningKeyForHS512AlgorithmWithMoreThan64Characters!!
app.jwt.expiration=3600000
app.jwt.refresh-expiration=86400000

# Email Configuration - FIXED
app.email.from-name=MySpot Team
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=venkat94727@gmail.com
spring.mail.password=llbtwxvdpubqrpnn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# OTP Configuration
app.otp.expiration-minutes=5
app.otp.max-attempts=3
app.otp.max-requests-per-hour=5

# Email Configuration
app.email.support-email=support@myspot.com
app.email.logo-url=https://myspot.com/logo.png
app.email.website-url=https://myspot.com

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.com.myspot=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Razorpay Configuration
razorpay.key.id=rzp_test_your_key_id
razorpay.key.secret=your_secret_key

# API Documentation (Swagger)
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

# CORS Configuration
cors.allowed.origins=http://localhost:3001
cors.allowed.methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed.headers=*

# Cache Configuration
spring.cache.type=simple

# Actuator (Health Check) Configuration
management.endpoints.web.exposure.include=health,info
#management.endpoint.health.show-details=when_authorized